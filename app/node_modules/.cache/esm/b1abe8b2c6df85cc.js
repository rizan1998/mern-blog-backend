let fs,path;_c6d‍.x([["fileRomover",()=>fileRomover]]);_c6d‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_c6d‍.w("path",[["default",["path"],function(v){path=v}]]);


const fileRomover = (filename) => {
  fs.unlink(path.join(__dirname, "../uploads", filename), function (err) {
    if (err && err.code == "ENOENT") {
      // file doesn't exist
      console.log(`File ${filename} doesn't exist won't remove it`);
    } else if (err) {
      console.log(`Error occured while trying to remove file ${filename}`);
    } else {
      console.log(`remove ${filename}`);
    }
  });
};


