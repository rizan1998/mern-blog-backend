let express,dotenv;_027‍.w("express",[["default",["express"],function(v){express=v}]]);_027‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();

const app = express();
app.use(express.json());

app.get("/", (req, res) => {
  res.send("server is running...");
});
