let express,dotenv;_352‍.w("express",[["default",["express"],function(v){express=v}]]);_352‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();

const app = express();
app.use(express.json());

app.get("/", (req, res) => {
  res.send("server is running...");
});
