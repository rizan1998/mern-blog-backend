let express,dotenv,connectDB,userRoutes;_866‍.w("express",[["default",["express"],function(v){express=v}]]);_866‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_866‍.w("./config/db",[["default",["connectDB"],function(v){connectDB=v}]]);_866‍.w("./routes/userRoutes",[["default",["userRoutes"],function(v){userRoutes=v}]]);



//Routes


dotenv.config();
connectDB();
const app = express();
app.use(express.json());

app.get("/", (req, res) => {
  res.send("server is running...");
});

app.user("/api/users", userRoutes);

const PORT = process.env.PORT || 5000;
app.listen(PORT, () => console.log(`Server is running in port ${PORT}`));
